<Page xmlns="http://schemas.nativescript.org/tns.xsd"
      actionBarHidden="true"
      loaded="onQuizListPageLoaded">
    <GridLayout rows="auto, *">
        <Label text="Choose Your Quiz"
               class="quizList-headerText imageBackgroundContainer" />
        <ListView items="{{ quizList }}" row="1">
            <ListView.itemTemplate>
                <GridLayout columns="auto, *, 100" class="quizList-itemContainer"> <!-- added 1 column -->
                    <Image src="{{ image ? image : 'res://unknownquizimage' }}"
                           class="quizList-image quizImage"
                           stretch="aspectFill"/>
                    <Label text="{{ name }}" col="1" class="quizList-text"/>
                    <!-- added label-->
                    <Label col="2" class="quizList-text quizList-quizAverage"
                           textWrap="true"
                           visibility="{{averageScoreExists? 'visible' : 'collapsed'}}">
                        <FormattedString>
                            <Span text="average:&#xa;"/>
                            <Span text="{{averageScore }}"
                                  fontWeight="Bold"/>
                        </FormattedString>
                    </Label>
                    <!-- added label-->
                    <Label col="2"
                           class="quizList-text quizList-quizAverage"
                           visibility="{{!averageScoreExists? 'visible' : 'collapsed'}}"
                           text="NEW!"/>
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</Page>